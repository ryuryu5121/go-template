version: '3.8'

services:
  app:
    build:
      context: .
      target: dev
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    command: ["air"]
    environment:
      - AIR_WATCH_DELAY=500
